import { default as InputIcon } from './Icon'
import { default as InputPassword } from './Password'

export const Inputs = Object.assign(
  {},
  {
    Password: InputPassword,
    Icon: InputIcon,
  }
)
