import { IInputsProps } from 'react-app-env'

import { ContainerPositionIcon } from './style'

export default ({ Icon }: IInputsProps) => {
  return <ContainerPositionIcon>{Icon}</ContainerPositionIcon>
}
