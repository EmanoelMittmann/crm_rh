import type { DefaultMetaProps } from './types'

const META_PROPS = {
  users: null,
  user_id: null,
  team: null,
  project_id: null,
  search: ''
} as unknown as DefaultMetaProps

export default { META_PROPS }
