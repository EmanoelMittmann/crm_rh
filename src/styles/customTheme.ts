import { extendTheme } from '@stardust-ds/react';

const theme = {
  color: {
    primary: {
      pure: 'red',
    },
    ubistart: 'blue',
  },
};

export const customTheme = extendTheme(theme);
